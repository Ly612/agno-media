[project]
name = "agno-media"
version = "0.1.0"
description = "Agno + AgentOS multi-agent service"
requires-python = ">=3.10"
dependencies = [
    "agno[os,sqlite]>=3.0.0",
    "openai>=1.0.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0.0",
]
